# MeetingAI uses credentials entered in the desktop app.
# Builds intentionally exclude VITE_* credentials, even if an old .env exists.
# Enter your Gemini key in Settings; it is stored in the OS credential vault.
# Local Whisper + Ollama mode needs no API key.
# Optional Google Calendar client configuration is entered in Settings.
